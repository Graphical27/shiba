# Open-Meteo is built in at /api/weather and requires NO URL or API token.
# Leave these blank to use Open-Meteo or the synthetic demo.
# Advanced: optional server-side custom normalized input feed.
# Never prefix secrets with NEXT_PUBLIC_.
# Payload format: docs/DATA_CONTRACT.md
FLOOD_INPUT_URL=
FLOOD_INPUT_TOKEN=
